---
- name: Bootstrap dotfiles prerequisites
  hosts: local
  gather_facts: true
  roles:
    - role: common
      tags:
        - always
        - common
    - role: prerequisites
      when: not (common_is_nixos | default(false) | bool)
      tags:
        - bootstrap
        - prerequisites
