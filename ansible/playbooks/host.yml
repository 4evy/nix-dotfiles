---
- name: Install dotfiles host-layer tools
  hosts: host_layer
  gather_facts: true
  roles:
    - role: common
      tags:
        - always
        - common
    - role: host_layer
      when: not (common_is_nixos | default(false) | bool)
      tags:
        - host
