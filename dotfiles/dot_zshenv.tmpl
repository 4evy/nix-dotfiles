# ░▀▀█░█▀▀░█░█░█▀▀░█▀█░█░█
# ░▄▀░░▀▀█░█▀█░█▀▀░█░█░▀▄▀
# ░▀▀▀░▀▀▀░▀░▀░▀▀▀░▀░▀░░▀░
# zshenv / quiet preflight
#      path[] ━━┳━━ wrappers
#               ┣━━ shared env
#               ┗━━ fpath[] after brew prefix
#  ⠁⠂⠄⡀ zsh reads this constantly, so no prompt tricks in here

# preflight 01 / path[] rail
#   zsh arrays keep the route readable and de-duplicated

typeset -U path PATH fpath FPATH
path=(
  "$HOME/.pi/agent/bin"
  "$HOME/.local/share/dotfiles/helix-tip/bin"
  {{- if eq .chezmoi.os "linux" }}
  "/home/linuxbrew/.linuxbrew/bin"
  "/home/linuxbrew/.linuxbrew/sbin"
  {{- else if eq .chezmoi.os "darwin" }}
  {{- if eq .chezmoi.arch "arm64" }}
  "/opt/homebrew/opt/coreutils/libexec/gnubin"
  "/opt/homebrew/opt/findutils/libexec/gnubin"
  "/opt/homebrew/opt/gnu-sed/libexec/gnubin"
  "/opt/homebrew/opt/grep/libexec/gnubin"
  "/opt/homebrew/opt/gawk/libexec/gnubin"
  "/opt/homebrew/opt/gnu-tar/libexec/gnubin"
  "/opt/homebrew/opt/gnu-which/libexec/gnubin"
  "/opt/homebrew/opt/diffutils/libexec/gnubin"
  "/opt/homebrew/opt/make/libexec/gnubin"
  "/opt/homebrew/opt/gnu-getopt/bin"
  "/opt/homebrew/bin"
  "/opt/homebrew/sbin"
  {{- else }}
  "/usr/local/opt/coreutils/libexec/gnubin"
  "/usr/local/opt/findutils/libexec/gnubin"
  "/usr/local/opt/gnu-sed/libexec/gnubin"
  "/usr/local/opt/grep/libexec/gnubin"
  "/usr/local/opt/gawk/libexec/gnubin"
  "/usr/local/opt/gnu-tar/libexec/gnubin"
  "/usr/local/opt/gnu-which/libexec/gnubin"
  "/usr/local/opt/diffutils/libexec/gnubin"
  "/usr/local/opt/make/libexec/gnubin"
  "/usr/local/opt/gnu-getopt/bin"
  "/usr/local/bin"
  "/usr/local/sbin"
  {{- end }}
  {{- end }}
  "$HOME/.local/bin"
  "$HOME/.bun/bin"
  "$HOME/.bun/install/global/node_modules/.bin"
  "$HOME/.cache/.bun/bin"
  "$HOME/.npm/bin"
  "$HOME/.cargo/bin"
  "$HOME/.go/bin"
  "$HOME/.yarn/bin"
  {{- if eq .chezmoi.os "linux" }}
  "/usr/local/bin"
  "/usr/bin"
  "/bin"
  "/usr/sbin"
  "/sbin"
  {{- else if and (eq .chezmoi.os "darwin") (eq .chezmoi.arch "arm64") }}
  "/usr/local/bin"
  "/usr/local/sbin"
  {{- end }}
  "${path[@]}"
)

# preflight 02 / NixOS wrappers
#   wrapper binaries get first crack at the command name

if [[ -d /run/wrappers/bin ]]; then
  path=(/run/wrappers/bin "${path[@]}")
fi
export PATH FPATH

# preflight 03 / shared env handoff
#   Homebrew, SSH agent, and tool defaults cross over from the shared file

if [[ -r "$HOME/.config/shell/environment.sh" ]]; then
  source "$HOME/.config/shell/environment.sh"
fi

# preflight 04 / completion sidecar
#   Homebrew completions join fpath after the prefix shows up

if [[ -n "${HOMEBREW_PREFIX:-}" ]]; then
  fpath=("$HOMEBREW_PREFIX/share/zsh/site-functions" "${fpath[@]}")
  export FPATH
fi
